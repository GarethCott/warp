//! Legacy "Warp AI" assistant panel was stripped from this fork; only the
//! shared execution-context type remains, since the dead-on-arrival agent
//! subtree still imports it.
pub mod execution_context;
